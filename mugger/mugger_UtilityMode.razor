// Set & Unset PK Mode
// set variable PkMode to true or false for use in other scripts
if varexist townMode
    @unsetvar townMode
    overhead "Housing Mode Off"
else
    @setvar townMode '1'
    overhead "Housing Mode On"
endif