if varexist debug
    sysmsg 'Mage.defense_routine.SummonTanks.razor'
    overhead 'Mage.defense_routine.SummonTanks.razor'
endif
if not varexist PKmode
    if followers < 4
    cast 'Earth Elemental'
        wait 3000
        if findtype 'mushroom' as m and diffmana > 50
            dclick m
            wait 2000
        endif
    endif
    if followers < 4
        cast 'Earth Elemental'
        wait 3000
        if findtype 'mushroom' as m and diffmana > 50
            dclick m
            wait 2000
        endif
    endif
    if followers = 4
        cast 'Summ. Creature'
        wait 2000
        if findtype 'mushroom' as m and diffmana > 50
            dclick m
            wait 2000
        endif
    endif
    wait 500
    skill 'spiritspeak'
else
    overhead 'In PK mode'
endif