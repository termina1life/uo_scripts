// Set & Unset PK Mode
// set variable PkMode to true or false for use in other scripts
if varexist Pkmode
    unsetvar! Pkmode
    overhead "Monster Mode"
else
    setvar! Pkmode 1
    overhead "Player Mode"
endif
