if extraMode and varexist myTarget
    overhead "removing my target"
    @unsetvar! setvar myTarget
elseif
    overhead "set mytarget"
    setvar myTarget lasttarget
elseif not targetexists 'harmful' #['any'/'beneficial'/'harmful'/'neutral']
    hotkey 'Last Target'
    if not targetexists 'harmful'
        if varexist PkMode
            //Prioritize the closest PK
            hotkey "Target Closest Non-Friendly Monster"
        else
            hotkey 'Target Closest Grey Monster'
        endif
    endif
endif