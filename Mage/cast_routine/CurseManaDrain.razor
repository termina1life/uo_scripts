if varexist debug
    sysmsg 'Mage.cast_routine.CurseManaDrain.razor'
    overhead 'Mage.cast_routine.CurseManaDrain.razor'
endif

if not varexist angryTarget
    overhead 'No angryTarget set, calling Targeting.'
    script 'Mage\masters\targeting'
endif
cast 'curse'
waitfortarget 
target angryTarget 
wait 500
cast 'mana drain'
waitfortarget 
target angryTarget 
wait 500
if diffmana > 50
    script 'Mage\utility\eatShrooms'
endif